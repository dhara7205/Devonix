// src/pages/Home.jsx
import React from "react";

export default function Home(){
  return (
    <main className="main">
      <div className="container">
        <div className="card">
          <h1 style={{margin:0}}>Welcome to Devonix</h1>
          <p style={{color:'var(--muted)'}}>Dashboard coming up — use Embed to add files, then Query to ask questions.</p>
        </div>
      </div>
    </main>
  );
}
